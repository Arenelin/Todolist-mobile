import {StyleSheet} from "react-native";

const s = StyleSheet.create({
    deleteContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 10
    },
    menuItemText: {
        color: '#fffffe',
        fontSize: 16,
        fontWeight: '500'
    }
})

export default s
