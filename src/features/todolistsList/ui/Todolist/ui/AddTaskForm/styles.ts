import {StyleSheet} from "react-native";

const s = StyleSheet.create({
    buttonCaption: {
        color: '#ffffff',
        textAlign: 'center'
    },
    button: {
        marginTop: 20,
        padding: 10
    },
    addTaskContainer: {
        maxWidth: 240,
        flexDirection: 'row',
        alignItems: 'center',
        gap: 10
    },
    input: {
        paddingTop: 16,
        paddingBottom: 16
    }
});

export default s
